function one() {
    const username = "Youtube"
    function two() {
        const webname = "Prabhat"
        console.log(username);
    }
    two()
}
// one()

if (true) {
    const username = "prabhat"
    if (username === "prabhat") {
        const website = "youtube"
        console.log(username + website)
    }
}

//++++++++++++++instersting+++++++++++++

function addone(num) {
    return num + 1
}
addone(5)

const addtwo=function(num){
     return num+2
}
addtwo(5)
